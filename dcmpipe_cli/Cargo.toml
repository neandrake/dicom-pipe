[package]
name = "dcmpipe_cli"
version = "0.1.0"
authors = ["neandrake <die.drachen@gmail.com>"]
edition = "2018"

[dependencies]
anyhow = "1.0"
bson = "2.2"
crossterm = "0.23"
cursive = { version = "0.17", default-features = false, features = ["crossterm-backend"] }
cursive_table_view = "0.14"
dcmpipe_dict = { path = "../dcmpipe_dict", version = "0.1" }
dcmpipe_lib = { path = "../dcmpipe_lib", version = "0.1", features = ["deflate"] }
mongodb = { version = "2.2", default-features = false, features = ["sync"] }
structopt = "0.3"
walkdir = "2.3"
